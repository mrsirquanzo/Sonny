{
  "name": "@sonny/eval",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "eval": "tsx src/index.ts"
  },
  "dependencies": {
    "@sonny/shared": "workspace:*",
    "@sonny/core": "workspace:*",
    "@sonny/mcp-gateway": "workspace:*"
  }
}
