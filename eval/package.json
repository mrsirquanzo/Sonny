{
  "name": "@sonny/eval",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@sonny/shared": "workspace:*",
    "@sonny/core": "workspace:*",
    "@sonny/mcp-gateway": "workspace:*"
  }
}
