# Plan Review Log: Sonny Analysis Toolbox

Started session 2026-07-16. MAX_ROUNDS=5. Reviewer: Codex gpt-5.6-sol (high), read-only.

## Round 1 — Codex

VERDICT: REVISE. 30 concrete flaws. Full critique summary:

Sandbox (1-8): subprocess fallback violates network-isolation criterion (must be Docker-only or fail-closed/cached-replay); ANARCI analogy is false (arbitrary model code, not a repo bridge); `--network none --read-only` is incomplete (needs rootless, digest-pinned image, --cap-drop=ALL, no-new-privileges, pinned seccomp, no IPC, unprivileged UID); `/work` is a dangerous host write surface (mount code read-only, size-bounded noexec tmpfs, validate artifacts by lstat/realpath/magic/hash); mounting whole lake breaks provenance (mount only allowlisted content-hashed files); container lifecycle/output caps underspecified (cidfile, kill in finally, cap logs/files/bytes/fds); isolation test too weak (Docker integration suite proving blocked IPv4/IPv6/DNS, immutable code/data, OOM/fork/timeout, zero orphans, skip fails release); runtime not reproducible (digest-pin base image + wheels, record image digest per computation).

Grounding (9-17): EvidenceKind lacks `computation` variant (contracts.ts) — build schema first; datasetId+codeHash+output is gameable (code can print a number without reading the file) — use reviewed parameterized templates for v1, defer unrestricted codegen; version is not immutable identity — use dataset content SHA-256 + query + timestamp + preprocessing version + manifest hash; code hash alone is not auditable — store exact source bytes + params + image digest + seed + dataset hashes + exit + output hashes; free-form stdout is not evidence and is injectable — require bounded schema-validated results.json; no binding claim->exact result — claim cites content-addressed computation ID + typed result keys, deterministically validate numeric tokens before LLM; decorrelated verifier does not check computation correctness — add deterministic computation-verification gate first; computation IDs can inflate GREEN in rag.ts (counts citation IDs) — compute independence from dataset content hashes; briefing.ts assembleReferences discards provenance — carry computation provenance through Briefing/CLI/UI.

Slices/integration (18-24): infra-before-science ordering risks sunk cost if TROP2 signal unusable — Slice 1 = scientific fixture spike; Slices 3&4 too big — reorder to (1) fixture (2) contracts/grounding/eval (3) Docker-only executor (4) template CLI (5) rendering+E2E; eval must be mandatory not optional; DataAnalysisSection risks bypassing existing Section[] path — use discriminated variant on existing pipeline; verifier seam ambiguous (deep research uses plain StructuredModel, decorrelated flag is patent path) — specify interface + decorrelated:false visible state; LUMINA not in this repo (web app uses runDossier) — name LUMINA contract or drop from 1-week criteria; mcp-gateway publishes only dist — define deployment bundle + packaged-install smoke test.

Science/demo (25-30): cBioPortal tumor vs GTEx TPM not comparable (batch/platform artifact) — use one jointly-processed compendium or prohibit cross-source quantitative comparison; GTEx bulk cannot localize epithelia or establish tox — limit claim to normal-tissue transcript signal, ground localization/tox separately; normal expression does not prove cleavable-linker preference — treat expression as therapeutic-window risk input, require literature for modality; DepMap dependency not decisive for ADC target + lacks lineage metadata — frame as orthogonal biology, add release-matched metadata, predefine thresholds; one-week scope unrealistic — one reviewed template over frozen TACSTD2 slices, one figure, three typed claims, cached fallback, defer general loop; keeping CDCP1 "warm" doubles work — one prevalidated case + precomputed backup artifact only.

### Claude's response

Accepted almost all. Revised the spec:
- Docker-only, hardened (cap-drop, no-new-privileges, seccomp, rootless, digest-pinned image, allowlisted-file mounts only, cidfile lifecycle, output caps). Removed unsafe subprocess fallback -> fail-closed + cached signed-replay. Removed the ANARCI security analogy.
- Added first-class `computation` evidence + schema-validated `results.json`; claim binds to content-addressed computation id + typed result key; deterministic recomputation/validation gate BEFORE the decorrelated LLM verifier; content SHA-256 provenance (not semver/date). Never send raw stdout to a model.
- v1 uses reviewed parameterized analysis templates (model selects params, not arbitrary code); general execute/observe codegen deferred to Phase 2 behind the deterministic gate.
- RAG independence computed from dataset content hashes, not citation ids.
- Reordered slices: (1) scientific fixture spike (2) contracts+grounding+MANDATORY eval (3) Docker-only executor (4) template CLI analysis (5) rendering+E2E. Eval no longer optional.
- Bounded the TROP2 scientific claims: expression = therapeutic-window risk input only; no tox/epithelial-localization/linker claims from bulk expression (grounded separately in literature); no cross-source quantitative tumor-vs-normal unless jointly processed; DepMap = orthogonal biology with release-matched lineage metadata + predefined thresholds, not a decisive ADC criterion.
- Removed LUMINA from the one-week hard criteria; CLI is the primary demo surface, LUMINA rendering is a named follow-on. Added deployment-bundle + packaged-install note.
- Dropped CDCP1 as a second live pipeline; precomputed backup artifact only.

Rejected/deferred: none outright. Unrestricted model-authored codegen is deferred (not rejected) to Phase 2 — v1 templates preserve a demonstrable "generates and runs sandboxed Python" story via parameterized instantiation while closing the gaming hole.

## Round 2 — Codex

VERDICT: REVISE. Opened with "resolves most round-one blockers, especially the unsafe fallback, whole-lake mount, free-form stdout, scientific overreach, and premature UI scope." 16 remaining, all refinements:
1 deterministic recomputation still optional -> mandatory double-execution within tolerance for live runs. 2 model-selected thresholds defeat preregistration -> lock thresholds/tissues/tests in template, model sets only target+question. 3 param injection surface -> byte-identical template, Zod-validated JSON via read-only file, host-side registry, spawn shell:false. 4 result schema too weak -> versioned scalar+grouped schemas with comparator/threshold/direction/precision/missingness/sampleN/nullable. 5 numeric-token parsing brittle -> structured claim bindings {computationId,resultKey,assertedValue,assertedUnit}. 6 evidence not discriminated -> backward-compatible discriminated union, computation branch requires all fields. 7 computationId construction unspecified -> sha256(JCS(canonical manifest)) versioned + golden vectors. 8 verification states conflated -> separate computationVerified/executionMode/llmVerdict/verifierDecorrelated, deterministic failure always drops. 9 RAG lacks seam -> source-identity resolver passed into computeRag. 10 artifact persistence undefined -> extract validated files to content-addressed host dir before container removal. 11 signed fallback no trust contract -> sign manifest+artifact hashes with release key, bundle pubkey, show executionMode:cached + recomputationVerified:false. 12 docker test gate conflicts with pnpm -r test -> separate protected test:docker job. 13 preprocessing provenance only a label -> dataset-lineage manifest (raw hashes + preprocessing code/image hash). 14 Phase 2 repeats grounding error -> requires independent trust mechanism, not just rerun. 15 framing overstates codegen -> "AI-planned execution of reviewed analytical methods". 16 GTEx too causal -> "flags potential normal-tissue exposure risk requiring protein-level and clinical confirmation".

### Claude's response
Accepted all 16. Rewrote spec: mandatory double-execution within tolerance; template-locked thresholds/tests (model sets only target+question); injection-safe Zod JSON params via read-only file + spawn shell:false; versioned scalar/grouped results.json schema; structured claim bindings; discriminated EvidenceSchema union; computationId=sha256(JCS(manifest))+golden vectors; separate verification-state fields with deterministic-failure-drops; RAG source-identity resolver; artifact extraction before container removal; signed cached-fallback trust contract; separate protected test:docker job; dataset-lineage manifest; Phase-2 independent-trust-mechanism note; honest "AI-planned execution of reviewed methods" framing; GTEx screening-flag phrasing.

## Round 3 — Codex

VERDICT: REVISE. 7 points, implementation-depth + 2 honesty fixes: 1 --read-only rootfs dropped -> restore + bounded tmpfs. 2 tmpfs output can't survive container removal -> persist output on a bind mount (not tmpfs), extract after exit. 3 computationVerified/recomputationVerified synonymous -> split originVerification (signed/cached) vs replayVerification (live double-run). 4 double execution is reproducibility not independent correctness -> say "re-executed reproducibly", correctness = template review + golden fixtures. 5 RAG per-file hash overcounts -> logical dataset-release group + parent-publication normalization. 6 image distribution unspecified -> registry pull by digest OR verified local build image ID. 7 discriminated Section union needs migration -> assign existing sections `research` discriminator + all producers/fixtures green in same slice.

### Claude's response
Accepted all substantive points. Fixed: --read-only rootfs + single RW bind-mounted output dir (persists) + tmpfs /tmp scratch; registry-digest-or-verified-local-image-ID; split executionMode/replayVerification/originVerification/llmVerdict/verifierDecorrelated (reproducibility != decorrelation); reworded to "re-executed reproducibly" with correctness = template review + golden fixtures (dropped "independently re-derived"); RAG logical dataset-release group + parent publication; Slice 2 ships the discriminated Section migration (research discriminator, all producers/fixtures green). Pure-mechanism items (PID-1 tmpfs supervisor, exact image-ID inspection procedure) noted as build-time decisions, not spec-level.

## Round 4 — Codex

VERDICT: REVISE. Opened "core architecture is now substantially sound." 5 points: 1 RW bind mount not size-bounded -> quota-backed FS + disk-flood test. 2 bind dir security contract -> fresh non-reused 0700 dir under trusted parent, nodev/nosuid/noexec, reject symlinked parents, delete unpromoted on exit. 3 cached signature != origin double-execution -> include originReplayVerification + two result hashes in signed manifest, sign only after gate passes. 4 "independent"/"correctness assured" wording still in goal/why/interview -> replace with "re-executed reproducibly" + "supported by" not "assured". 5 no Docker on this machine -> add Slice-0 preflight proving rootless runtime + pinned image + mounts + network + resource controls on the actual demo machine.

### Claude's response
Accepted all. Fixed: output dir now fresh 0700 non-reused under trusted parent, quota/size-capped, nodev/nosuid/noexec, reject symlinked parents, delete unpromoted on every exit path + disk-flood test; cached signing only after reproducibility gate passes, signed manifest includes originReplayVerification + two result hashes; replaced remaining "independently re-derived" with "reproducibly re-executed" and "assured" -> "supported by" everywhere; added Slice 0 runtime preflight (rootless Docker + pinned image + mounts + network + resource controls proven on the demo machine before implementation).

## Round 5 — Codex

VERDICT: APPROVED. "The spec is now sound enough to implement. The sandbox is fail-closed and testable, computed claims have enforceable provenance and reproducibility contracts, cached evidence has a defined trust chain, the slices are correctly ordered, and the one-week scope is credible if Slice 0 passes immediately." Two non-blocking editorial nits (line 84 "assured"->"supported"; status line) fixed post-approval.

## Outcome: CONVERGED in 5 rounds (30 -> 16 -> 7 -> 5 -> APPROVED). Awaiting human sign-off (gate #2) before any code.
